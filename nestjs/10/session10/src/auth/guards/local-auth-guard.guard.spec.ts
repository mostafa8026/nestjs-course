import { LocalAuthGuardGuard } from './local-auth-guard.guard';

describe('LocalAuthGuardGuard', () => {
  it('should be defined', () => {
    expect(new LocalAuthGuardGuard()).toBeDefined();
  });
});
